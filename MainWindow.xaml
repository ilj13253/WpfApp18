<Window x:Class="WpfApp18.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp18"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock x:Name="FileName" Text="{Binding FileName}"/>
        <Button Content="" Command="{Binding StartCommand}" Width="75" Height="29" Margin="359,0,359.6,0"/>
        <Label x:Name="Result" Content="{Binding Result}">
        </Label>
        <Label x:Name="ListResult" Content="{Binding ListResult}"></Label>
        <ListBox x:Name="ListRes" ItemsSource="{Binding ListResult}">
        </ListBox>
    </StackPanel>
</Window>
